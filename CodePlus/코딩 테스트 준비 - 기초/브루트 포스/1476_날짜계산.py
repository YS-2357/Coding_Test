# 백준 1476번: 날짜 계산
# 문제 설명:
# 1년부터 시작하여 주어진 (E, S, M)을 처음 만족하는 연도를 찾는 문제.
# - 지구(E) 1 ≤ E ≤ 15
# - 태양(S) 1 ≤ S ≤ 28
# - 달(M) 1 ≤ M ≤ 19

# 입력 형식:
# 한 줄에 세 개의 정수 E, S, M이 주어진다.

# 출력 형식:
# 주어진 E, S, M을 만족하는 가장 빠른 연도를 출력.

# 예제 입력 1:
# 1 16 16
# 예제 출력 1:
# 16

# 예제 입력 2:
# 1 1 1
# 예제 출력 2:
# 1

# 예제 입력 3:
# 15 28 19
# 예제 출력 3:
# 7980

# 🛑 [❌ 사용자가 작성한 코드] (틀린 코드)
"""
E, S, M = map(int, input().split())

year = 1
while True:
    if ((year - 1) % 15 + 1 == E) & ((year - 1) % 28 + 1 == S) & ((year - 1) % 19 + 1 == M):  # ❌ 논리 연산자 사용 오류
        print(year)
        break
"""

# 📌 [❌ 사용자의 코드에서 틀린 점]
# 1. **논리 연산자 사용 오류**
#    - `&`(비트 연산자)가 아니라 `and`(논리 연산자)를 사용해야 함.
#    - `&`는 비트 연산자로, `and`와 다르게 동작함.
# 2. **수식을 간결하게 정리 가능**
#    - `(year - 1) % X + 1 == Y` 형태를 유지할 필요 없음.
#    - `year % 15 == E % 15`처럼 `year`를 직접 비교하는 방식으로 단순화 가능.

---

# ✅ [✔ 모범 답안: 올바르게 수정된 코드]
# - `year % 15 == E % 15` 형식으로 조건을 단순화.
# - `&` 대신 `and`를 사용하여 논리 연산을 정확하게 수행.

E, S, M = map(int, input().split())

year = 1
while True:
    if year % 15 == E % 15 and year % 28 == S % 28 and year % 19 == M % 19:
        print(year)
        break
    year += 1
