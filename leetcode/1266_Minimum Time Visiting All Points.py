# 1266_Minimum Time Visiting All Points.py
# -----------------------------------------------------
# ✅ 제목: LeetCode 1266. Minimum Time Visiting All Points
# 🏷️ 유형: 그리디 / 기하
#
# ✅ 문제 설명(요약):
#   - 2차원 평면 위의 여러 점들을 주어진 순서대로 모두 방문한다.
#   - 한 번에 상하좌우 또는 대각선으로 1만큼 이동할 수 있다.
#   - 모든 점을 방문하는 데 필요한 최소 시간을 계산한다.
#
# ✅ 입력 형식(요지):
#   - points: 각 점의 좌표를 담은 리스트 [[x1, y1], [x2, y2], ...]
#
# ✅ 규칙 요약:
#   - 현재 점에서 다음 점으로 이동할 때, 대각선 이동이 가능하다.
#   - 이동 시간은 이동 횟수와 동일하다.
#
# 🧠 핵심 불변식(Invariant):
#   - 두 점 (x1, y1) → (x2, y2) 사이의 최소 이동 시간은
#     max(|x1 - x2|, |y1 - y2|) 이다.
#   - 각 구간의 최소 이동 시간을 독립적으로 더하면 전체 최소 시간이 된다.
#
# ✅ 정답 코드(나의 풀이; 절대 수정 금지)

class Solution:                                                     # LeetCode 제출 형식에 맞춘 Solution 클래스 정의
    def minTimeToVisitAllPoints(self, points: List[List[int]]) -> int:  # 모든 점을 방문하는 최소 시간을 계산하는 함수
        ans = 0                                                     # 전체 이동 시간을 누적할 변수 초기화
        n = len(points)                                             # 점의 개수를 저장

        if n == 1:                                                  # 점이 하나뿐인 경우
            return 0                                                # 이동이 필요 없으므로 0 반환

        for i in range(n - 1):                                      # 인접한 점 쌍을 순회
            p0, p1 = points[i], points[i + 1]                       # 현재 점과 다음 점을 각각 저장
            dist = max(abs(p0[0] - p1[0]), abs(p0[1] - p1[1]))      # 체비셰프 거리로 최소 이동 시간 계산
            ans += dist                                             # 해당 구간의 이동 시간을 전체 합에 누적

        return ans                                                  # 모든 점을 방문하는 데 필요한 최소 시간 반환

# -----------------------------------------------------
# 🔍 첫 시도 결과:
#   - 첫 제출에서 정답 처리됨 (Accepted).
#
# 🔧 오답 이유 및 사용한 알고리즘 개념:
#   - 오답 없음.
#   - 사용 개념: 체비셰프 거리 기반 그리디 계산.
#
# 📚 시간·공간 복잡도:
#   - 시간: O(n)  (점의 개수만큼 한 번 순회)
#   - 공간: O(1)  (상수 공간 사용)
#
# -----------------------------------------------------
# (선택) 다른 효율적 풀이 또는 알고리즘 제안:
#   - 각 이동을 x축, y축 이동으로 분해해도 동일한 결과가 나오며,
#     결국 대각선 이동을 최대한 활용하는 것이 최적임을 확인할 수 있다.
