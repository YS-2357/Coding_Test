# 1526_Minimum_Number_of_Increments_on_Subarrays_to_Form_a_Target_Array.py
# -----------------------------------------------------
# ✅ 제목: LeetCode 1526. Minimum Number of Increments on Subarrays to Form a Target Array
# ✅ 문제 설명(요약):
#   초기 배열이 [0, 0, ..., 0]일 때,
#   연속된 구간(subarray)에 +1을 하는 연산으로 target 배열을 만들어야 한다.
#   가능한 연산의 최소 횟수를 구하라.
#
# ✅ 입력 형식(요지):
#   - target: 정수 리스트 (길이 n)
#
# ✅ 규칙 요약:
#   - 한 번의 연산으로 연속된 구간의 모든 원소를 +1 할 수 있다.
#   - 이전 값보다 이후 값이 더 커지는 순간마다 새 연산이 필요하다.
#   - 따라서, 처음 값(target[0])과 모든 “증가 구간의 차이”를 더하면 된다.
#
# ✅ 정답 코드(나의 풀이; 절대 수정 금지)
class Solution:
    def minNumberOperations(self, target: List[int]) -> int:
        n = len(target)
        ans = target[0]                     # 첫 번째 원소만큼 초기 연산 필요
        for i in range(n-1):
            prev, nxt = target[i], target[i+1]
            if prev < nxt:                  # 이전보다 이후가 커졌을 때만 증가분 추가
                ans += nxt - prev
        return ans
# -----------------------------------------------------
# 🔍 첫 시도 결과:
#   - 모든 테스트케이스 통과.
#   - 증가하는 순간만 누적합을 더하는 방식으로 최소 연산 수를 정확히 계산.
#
# 🔧 오답 이유 및 사용한 알고리즘 개념:
#   - 알고리즘 개념: Greedy (증가 구간만 새 연산 발생)
#   - 직관: 이전보다 높아지는 순간에만 새로운 subarray 증가가 필요.
#   - 감소 구간은 이미 만들어진 값이 줄어드는 것뿐이라 추가 연산 불필요.
#
# 📚 시간·공간 복잡도:
#   - 시간복잡도: O(n)  (한 번의 순회)
#   - 공간복잡도: O(1)  (상수 변수만 사용)
# -----------------------------------------------------
# (선택) 다른 효율적 풀이 또는 알고리즘 제안:
#   - 같은 로직을 한 줄로 표현 가능:
#       return target[0] + sum(max(0, target[i] - target[i-1]) for i in range(1, len(target)))
#   - 원리 동일, 코드 간결화(O(n) 동일).
