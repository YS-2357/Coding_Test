# 3074_Apple_Redistribution_into_Boxes.py
# -----------------------------------------------------
# ✅ 제목: LeetCode 3074. Apple Redistribution into Boxes
# ✅ 문제 설명(요약):
#   - 여러 상자에 담긴 사과 개수 apple과, 빈 상자들의 수용량 capacity가 주어진다.
#   - 모든 사과를 빈 상자들로 옮겨 담으려 할 때, 필요한 최소 상자 개수를 구한다.
#   - 상자는 수용량만큼 담을 수 있으며, 사과는 합계 기준으로 모두 담기기만 하면 된다.
#
# ✅ 입력 형식(요지):
#   - apple: 각 기존 상자에 들어있는 사과 개수 리스트
#   - capacity: 사용할 수 있는 빈 상자들의 수용량 리스트
#
# ✅ 규칙 요약:
#   - 목표는 사과의 총합(sum(apple))을 capacity의 일부 상자 합으로 커버하는 최소 개수 찾기.
#   - 가장 큰 수용량 상자부터 채우는 그리디가 최적:
#     - 같은 개수의 상자를 쓰면 총 수용량을 최대화하므로,
#     - 필요한 개수를 가장 빨리(최소 개수로) 충족한다.
#
# ✅ 정답 코드(나의 풀이; 절대 수정 금지)
#   - 아래는 사용자가 제출/채택한 최종 정답 코드이며,
#     이 단계에서는 코드 내용을 변경하지 않고,
#     각 줄마다 설명 주석만 추가한다.

class Solution:
    def minimumBoxes(self, apple: List[int], capacity: List[int]) -> int:
        capacity.sort(reverse=True)                      # 수용량을 내림차순 정렬(큰 상자부터 사용)
        sum_apple = sum(apple)                           # 옮겨야 하는 사과 총합 계산

        for i, c in enumerate(capacity):                 # 큰 수용량 상자부터 하나씩 사용
            sum_apple -= c                               # 현재 상자의 수용량만큼 사과를 담는다고 가정(남은 사과 감소)
            if sum_apple <= 0:                           # 남은 사과가 0 이하이면 모두 담을 수 있음
                return i + 1                             # 사용한 상자 개수(0-index이므로 i+1) 반환

# -----------------------------------------------------
# 🔍 첫 시도 결과:
#   - (기록 없음) 본 그리디 풀이로 최소 상자 개수 계산 가능.
#
# 🔧 오답 이유 및 사용한 알고리즘 개념:
#   - 사용한 핵심 개념:
#     - 그리디(내림차순 정렬 후 큰 것부터 누적)
#     - 전체 사과 합을 가장 큰 capacity들로 빠르게 커버하여 최소 개수를 보장
#   - 흔한 실수 포인트(일반적):
#     - capacity를 오름차순으로 사용해 불필요하게 많은 상자를 선택
#     - 사과를 개별로 “배치”하려고 과하게 시뮬레이션(필요 없음: 합만 맞으면 됨)
#
# 📚 시간·공간 복잡도:
#   - 시간 복잡도: O(k log k)
#     - k = len(capacity), 정렬 비용이 지배적
#   - 공간 복잡도: O(1) 추가 (정렬은 제자리 수행, 입력 제외)
#
# -----------------------------------------------------
# (선택) 다른 효율적 풀이 또는 알고리즘 제안:
#   - capacity를 정렬한 뒤 prefix sum을 만들고, 이진 탐색으로 최소 인덱스를 찾는 방식도 가능하나
#     본 문제 크기에서는 현재 그리디가 가장 단순하고 충분히 빠르다(코드 X).
